import { ComingSoonScreen } from '@/screens/coming-soon';

export function Settings() {
  return (
    <ComingSoonScreen
      title="Settings"
      description="Reminders, backup/export, reduced motion, and appearance settings land in a later checkpoint."
    />
  );
}
